
    .article-hero::after{
      content:"";
      position:absolute;
      inset:0;
      background:
        radial-gradient(circle at 82% 20%, rgba(96,165,250,.22), transparent 28%),
        radial-gradient(circle at 74% 78%, rgba(34,197,94,.14), transparent 24%);
      z-index:1;
      pointer-events:none;
    }