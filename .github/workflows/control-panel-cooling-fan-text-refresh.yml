name: Cooling Fan Text Refresh

on:
  push:
    branches:
      - preview-control-panel-cooling-fan-text-refresh-20260916

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview-control-panel-cooling-fan-text-refresh-20260916
          fetch-depth: 0
      - name: Apply refresh
        run: python .github/scripts/control_panel_cooling_fan_refresh.py
      - name: Commit result
        run: |
          rm -f .github/workflows/control-panel-cooling-fan-text-refresh.yml
          rm -f .github/scripts/control_panel_cooling_fan_refresh.py
          git config user.name denkicontrol-preview-writer
          git config user.email actions@users.noreply.github.com
          git add articles/control-panel-cooling-fan-basic.html docs/existing-article-improvement-backlog.md .github/workflows/control-panel-cooling-fan-text-refresh.yml .github/scripts/control_panel_cooling_fan_refresh.py
          git commit -m "Refresh control panel cooling fan article text and related cards"
          git push origin HEAD:preview-control-panel-cooling-fan-text-refresh-20260916
