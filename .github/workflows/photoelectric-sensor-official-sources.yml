name: Photoelectric Sensor Official Sources
on:
  push:
    branches: [preview-photoelectric-sensor-official-sources-20260916]
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview-photoelectric-sensor-official-sources-20260916
      - name: Apply official source box
        run: python scripts/photoelectric-sensor-official-sources.py
      - name: Clean temporary automation and commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/photoelectric-sensor-official-sources.yml scripts/photoelectric-sensor-official-sources.py
          git add articles/photoelectric-sensor-basic.html
          git commit -m "Add official sources to photoelectric sensor article"
          git push
