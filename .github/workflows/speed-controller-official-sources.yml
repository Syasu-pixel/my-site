name: Speed Controller Official Sources
on:
  push:
    paths:
      - '.github/workflows/speed-controller-official-sources.yml'
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview-speed-controller-official-sources-20260916
      - name: Apply official source box
        run: python scripts/speed-controller-official-sources.py
      - name: Clean temporary automation and commit
        run: |
          rm -f scripts/speed-controller-official-sources.py .github/workflows/speed-controller-official-sources.yml
          git config user.name "Syasu-pixel"
          git config user.email "syasuta0819@gmail.com"
          git add -A
          git commit -m "Add manufacturer sources to speed controller article"
          git push
