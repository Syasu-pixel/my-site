# Denkicontrol 作業開始ガイド

## 全ルール確認の共通入口

新しいチャット・引き継ぎ・「ルールを確認して」の依頼では、[ルール確認手順](docs/rules/README.md)と正式一覧を先に開く。開始時必須を読み、条件付き全件の適用性を判定し、必要な参照先まで確認する。未読・取得不可・適用性未判定を残して「全ルール確認済み」と言わない。個別業務の実行順は本書の該当節に従う。

対象は `Syasu-pixel/my-site`。このファイルは作業の入口であり、安全原則や管理者の明示指示を上書きしない。

## 新しいチャット・引き継ぎ時
1. 現在のユーザー依頼、対象ブランチ、mainとの差分、既存PR・未完了作業を確認する。過去チャットの記憶だけで更新しない。
2. `docs/ai-governance.md` と `docs/ai-editorial-master-rules.md` を読む。
3. サイト構造、ヘッダー、カード、CSS、記事作成、テンプレート、生成・公開方式に関係する場合、`docs/site-template-policy.md` と `docs/site-template-migration-status.md` を読む。
4. 続いて `docs/agent-runbook.md` と、その作業に関係する正本を読む。監査は `docs/article-audit-entrypoint.md`、既存記事の内容変更は日付ガード・検索データ事前確認へ進む。
5. 実装前に「ルール案／採用済み方針／実装済み／本番反映済み」を区別する。共通化の文書があるだけで移行済みと判断しない。

## 共通ひな形移行の境界
- 状態の正本は `docs/site-template-migration-status.md`。管理者のルール採用判断と実装依頼の範囲を確認してから、別の実装PRへ進む。既に明示された依頼・承認は引き継ぎ、同じ確認を繰り返さない。
- 共通部品、ページ種別ごとのレイアウト、記事固有データを分ける。通常記事へ相談窓口・法務ページ・設計シリーズを強制変換しない。
- 本文、URL、画像、SEO、言語切替、支援リンク、検索、評価カード等の既存要件を保存する。
- 生のテンプレートを公開せず、公開前にHTMLを生成する。実装方式・ソース配置・移行状態は `docs/site-template-migration-status.md` で確認する。
- 根拠のない承認済み・テスト済み・移行済みの記載をしない。

## 文書の更新
移行の状態・採用記録・件数は移行状況文書だけで更新する。この入口には現在段階を重複記載しない。
別サービスのChatGPT等がこのファイルを自動読込するとは限らないため、runbook・マスタールールからも同じ方針へリンクする。文書追加だけで全実行環境に強制適用されるとは主張しない。
