# トップページ修正設計（監査ベース）

対象: `index.html` のみ  
方針: 大改装はせず、既存セクションの見出し・導線文言・カード内短文を差し替えて「カテゴリ一覧サイト」から「作業内容で選ぶサイト」へ寄せる。

## 修正方針の要約

- **入口の判断軸を「工具名」から「作業工程」へ変更**する。  
  （切る→剥く→圧着→確認 の順で読むだけで進める導線へ）
- **既存カード/既存リンクを活かしたまま、見出し・補助文だけ更新**し、最小差分で意図を伝える。
- **信頼感優先の型**（まず1本 / 追加枠 / 注意点 / 向いていないケース）をトップにも薄く実装し、詳細記事との体験を揃える。

---

## 直すべきポイント

### 1) H2・導線文言（カテゴリ一覧 → 作業内容で選ぶ）

- ヘッダーナビ `工具カテゴリー一覧` は、**工程起点の文言**へ変更。
  - 例: `作業内容から探す`
- パンくず `主要工具カテゴリ一覧` は、**作業軸の名称**へ変更。
  - 例: `作業内容別ガイド`
- カテゴリセクションのアイブロウ `カテゴリ一覧` は、**作業フロー一覧**へ変更。
  - 例: `作業フローで探す`
- H2 `用途別に工具を探す` は、**工程別に必要工具を選ぶ**へ変更。
  - 例: `作業工程ごとに必要な工具を選ぶ`

### 2) 「ニッパー→ストリッパー→圧着工具」導線（工程名ベース）

既存の順序は活かし、見え方だけ工程名へ寄せる。

- クイックナビ文言を工程名に置換
  - `切る（ニッパー）`
  - `剥く（ストリッパー）`
  - `圧着する（圧着工具）`
  - 既にある `確認する（テスター）` を明示的に4番目として追記
- 「迷ったら、この順で…」セクションの見出しを工程名化
  - 例: `迷ったら「切る→剥く→圧着→確認」の順で見る`
- 各カードH3を「工程名 + 工具名」へ変更
  - 例: `01 切る（ニッパー）`

### 3) カテゴリカードに「向いていないケース」を短文追加

各カードに1行追加（過度に長くしない）。

- ニッパー: `※ 太物幹線の切断中心ならケーブルカッター優先。`
- ストリッパー: `※ 皮むき頻度が低いならナイフ中心でも可。`
- 圧着工具: `※ 太物圧着中心なら油圧/電動圧着機を先に確認。`
- テスター: `※ 絶縁抵抗測定が主目的ならメガーを優先。`

> 追加位置は既存 `<p>` 末尾に `<span class="not-fit">…</span>` を追記するのが最小差分。

### 4) 上部に「まず1本 / 追加枠 / 注意点」の共通判断ブロック

ヒーロー直下（または既存 `section-mobile-compact` 上段）に、3列の短い判断ブロックを追加。

- タイトル例: `最初に決める3点`
- 3項目:
  1. `まず1本`: 出番最大工程（多くは「切る」）から決める
  2. `追加枠`: 次に失敗コストが高い工程（剥く/圧着）を追加
  3. `注意点`: 対象線材・端子規格・測定要件を先に確認

> 新規セクションを1つ追加するが、既存の `.value-grid` / `.value-card` を再利用してCSS差分を抑える。

### 5) カテゴリの束ね方を“作業単位”へ寄せる最小差分

カード自体の並び替えは最小限にし、**グルーピング見出しだけ追加**する。

- 先頭4カードを `基本工程` として明示（切る/剥く/圧着/確認）。
- その下を `周辺作業`（通線・支持・搬送・加工など）として整理。
- 実装は、既存グリッドの間に小見出し（`h3`相当）を挿入する方法が最小。

---

## 変更案（実装時の差分イメージ）

1. **テキスト差し替え中心**
   - ヘッダーリンク、パンくず、H2、導線文を工程起点へ修正。
2. **導線の4工程化**
   - 既存3工程導線に `確認（テスター）` を追加。
3. **カードに短い注意行を追加**
   - 「向いていないケース」を1行だけ追記。
4. **共通判断ブロック追加**
   - 既存カードコンポーネントを流用し、上部に3項目を追加。
5. **グルーピング見出しの挿入**
   - `基本工程` / `周辺作業` の区切りを入れる（カード本体は大きく触らない）。

---

## 期待できる効果（監査観点）

- 初見ユーザーが「何を買うか」より**「今どの工程で困っているか」**で入口判断しやすくなる。
- 既存の詳細記事（ratchet-wrench / hydraulic-punch / ladder-work-platform で採用した型）と、トップ導線の思想が一致する。
- レイアウト再構築なしで、**文言と小ブロック追加だけで意図を変えられる**。
