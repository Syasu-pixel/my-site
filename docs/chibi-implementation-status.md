# ちびキャラ実装状況と会話検証

2026-09-08

- 基本動作カタログ218項目は設計。全実装ではない。
- 素材は各キャラ4セル（通常・口開き・目閉じ・手上げ）。現ランタイムは通常セルのみ表示。全身セルを切り替えて以前のぶれを再発させない。
- 実装済みは透過素材、下端での座標移動、接地影、会話吹き出し、左余白の簡易テーブル、停止・OFF。
- 脚運び、顔パーツの独立口パク、着座、家具搬入、給湯、梯子の登降は未実装。

## 今回追加した会話

chibi-dialogues.js に編集・調査の考え方136組、休憩雑談24組、合計160組・320発言を手書きで追加。語句を置換した組み合わせ数ではない。内容には近いテーマもあるため、160種類の小芝居とは呼ばない。

通常案件中は136組（約125分の55秒枠）、待機中は160組（約147分の55秒枠）を利用できる。各組は先輩8秒＋後輩8秒で、その後は静かな時間。発話そのものが2時間続くわけではない。

候補は未使用・最も古いものを優先し、同一IDを全モード共通で7200000ミリ秒除外。履歴をlocalStorageへ保存。確認待ち・エラー・履歴閲覧中は状況に対応する限定会話にして、正常稼働を演出しない。これらの状態に2時間分の会話は用意していない。複数タブ間の同時選択排他は未対応。

## 検証

実装したchooseSpeech関数を抽出し、時計と保存先のみ置換して実行。待機固定・仕事固定・待機/調査/制作/仕事の切替の3ケースそれぞれで、55秒間隔の132枠（開始0秒〜7205秒）について候補切れなし・同文重複なし。データ全160組の全文重複なし。JS構文確認済み。実ブラウザでの2時間連続観察は未実施。
