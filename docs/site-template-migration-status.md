# 共通ひな形移行の状態・引き継ぎ

- 更新日: 2026-09-18
- フェーズ: RULES_PROPOSED
- 方針: [site-template-policy.md](site-template-policy.md)
- 調査基準main: `25115cf61fade37dd6c52a7f9071286e87d77255`
- 管理者からの指示: 見た目を保ち後編集を容易にする共通化を検討。実装より先に既存ルールを横断確認し、今回のルールをGitHubへ明文化する。
- 管理者申告: この作業開始時、他のチャットで進行中の作業はない。将来の再開時は改めてPR・ブランチを確認する。

## 完了／未完了
- 全309 HTMLの構造パターン調査、Markdown85件の取得・横断確認、関連正本の詳細確認を実施。
- 文書案15件をレビュー。入口の状態重複を解消し、初回移行・新規記事の適用条件を明記。ルール最終採用は未記録。
- 2026-09-18にGitHubの最新mainを再確認し、調査基準commitとの一致を確認。
- 同日に未完了PRを46件確認。別チャットの稼働状態とは区別し、本作業では既存PRを変更・整理しない。共通ひな形方針と同目的のPRは一覧上見当たらない。
- 実装方式・正本ディレクトリ・データ形式は未決定。
- 移行済みページ: **0件**。本番の構造変更: **未実施**。
- ローカルに作成した試作コードは採用済み実装ではなく、この文書PRへ含めない。

## 次の作業
1. この文書PRの方針・例外・既存ルールとの関係を確認する。
2. 管理者のルール採用判断を、対象PR／commitとともに記録する。
3. 採用後に限定したパイロットの構成・検査・Preview方法を具体化する。
4. 実装依頼の対象範囲を確認して進む。既に受けた明示依頼を重複確認しない。現状態から全ページの移行や本番マージへ飛ばない。

## 移行台帳に今後記録する項目
対象URL、ページ種別、移行状態、旧ソース、新正本、使用レイアウト／部品、生成先、例外理由、変更前commit、検査、Preview、管理者判断、本番反映、戻し方。
未登録ページを自動的に移行済みと扱わない。

## 引き継ぎの更新義務
各段階の終了時に本書の状態・履歴を更新する。`AGENTS.md` と既存入口には現在段階を重複記載しない。方針採用時はpolicyの提案表記とDecisionのStatusも更新する。PR作成／ルール採用／実装検証／本番反映を別々に記録し、過去の状態を削除して完了に見せない。
