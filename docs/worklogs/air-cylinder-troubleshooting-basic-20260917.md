# エアシリンダートラブル記事 改善作業ログ / 再開用バックログ

更新日: 2026-09-17
状態: **一時停止 / 再開待ち**
対象記事: `articles/air-cylinder-troubleshooting-basic.html`
作業PR: #1488
作業ブランチ: `preview-air-cylinder-troubleshooting-official-sources-20260916`
main: **未反映。ユーザー承認前にマージしない。**

## 今回の目的

既存記事をむやみに長文化することが主目的ではない。古い画像・古い技術表現を現在の品質基準へ更新し、本文と画像の説明内容を一致させる。必要情報が不足している場合のみ本文を補足する。

記事冒頭の「メーカー公式資料を確認して作成しています」ボックスの見せ方は好評で、この方向を維持する。

## 現在までの主な判断

- メーカー公式資料の確認情報を記事冒頭に表示する構成は維持。
- 画像は本文の見出し・説明と一致させる。本文と無関係な画像や、同じ内容を重複説明する画像は置かない。
- 技術図は見栄えより技術的整合性を優先する。エアホース、A/B/Pポート、シリンダ接続位置などに矛盾が出る場合は、無理に全配管をつながず、構成要素を分けて見せる。
- キャラクター（先輩・後輩）は既存の採用イメージを基準に固定し、画像ごとに別人化させない。
- 採用済み画像を理由なく再生成しない。ただし今回、旧採用候補より新生成版のほうが説明性が高かったため、Step 5〜7について採否を更新した。

## Step 5〜7 画像の最新採否

### Step 5: スピコンの絞りすぎ・向きを見る

**未完了 / 要再生成。**

最新案の世界観・レイアウト・説明の分かりやすさは維持する。
修正必須点:

- シリンダのA/B側ポート位置を技術的に自然な位置へ修正する。
- 継手・エア配管がシリンダロッドへ突き刺さって見える構造を禁止。
- A/Bポートとシリンダ端部の関係を破綻させない。
- アウト絞り（排気絞り）が基本という説明方針を維持する。
- 両側へ同時に供給・排気しているように誤読される空気経路は避ける。
- 技術的に複雑になる場合、全エアホースを無理につながず概念図として分離して見せる。

停止理由: ChatGPT Plusの画像生成上限に到達したため、この1枚の再生成前に作業停止。

### Step 6: 機械干渉・ロッドの動きを見る

**最新生成版を採用。**

旧版ではなく、今回生成した説明図寄りの最新版を採用する。シリンダ中央へ不自然なホースが刺さる旧案などは不採用。

### Step 7: リードスイッチ位置と検出状態を見る

**最新生成版を採用。**

取付位置 → LED → 配線 → PLC入力モニタ（例）の切り分けが分かりやすい最新版を採用する。

## Previewで確認された未完了事項

1. Step 5「スピコンの絞りすぎ・向きを見る」の画像を上記条件で再生成・採用確定する。
2. Step 6「機械干渉」とStep 7「リードスイッチ」の最新採用画像を記事へ正しく配置し、Previewで画像表示を確認する。
3. 「合わせて読みたい記事」を、文字だけのカードではなく**各既存記事のOGP画像を使った写真付きカード**へ修正する。
4. 記事内で画像が表示されない箇所が残っていないか、Step 5〜7を中心にPC/スマホで目視確認する。
5. 本文と画像の説明が重複しすぎていないか、また本文と画像が別内容になっていないか最終監査する。
6. 不足情報がある場合だけ本文を追加し、画像更新を理由に不要な長文化はしない。
7. 最終Preview承認までは `main` に反映しない。英語版 `en/` は触らない。

## Preview / Cloudflareメモ

PR #1488 のCloudflare実デプロイは成功済み。
Cloudflare Botが返したBranch Preview aliasは `preview-air-cylinder-trouble.denkicontrol-preview.pages.dev`。

GitHub Actionsの `ai-editorial-preview-capture.yml` はブランチ名からPreview URLを推測生成しており、実際のCloudflare aliasと一致しない場合がある。Preview URLは推測値ではなく、Cloudflare BotがPRへ返した実URLを優先する。

## 再開時の最初の作業

1. このファイルを読む。
2. PR #1488 と現在のPreview差分を確認する。
3. Step 5のスピコン画像だけを再生成する。Step 6/7は再生成しない。
4. Step 5を技術監査して採否を決める。
5. Step 5〜7の採用画像を配置。
6. 「合わせて読みたい記事」をOGP画像付きカード化。
7. Cloudflareの実Preview URLでPC/スマホ目視確認。
8. ユーザー承認後にのみ本番反映を検討する。

## 再開用の短い指示

新しいChatGPTチャットでは、次のように依頼すればよい:

> `Syasu-pixel/my-site` の `docs/worklogs/air-cylinder-troubleshooting-basic-20260917.md` と PR #1488 を読んで、エアシリンダートラブル記事の作業を続きから再開してください。mainにはまだ反映しないでください。

このworklogを再開時の正本として扱い、チャット履歴だけに依存しないこと。
