[build]
  publish = "."

# Netlify is used only as a review/preview surface for this repository.
# Canonical Production remains GitHub Pages on https://denkicontrol.com/.
# Keep every Netlify-served response out of search indexes, including the
# duplicate Netlify main deployment created by the Git integration.
[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, noarchive"
    Referrer-Policy = "no-referrer"
